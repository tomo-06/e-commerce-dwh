e_commerce_dwh:
  target: dev
  outputs:
    dev:
      type: snowflake
      role: SYSADMIN # 学習用。実務では最小権限の専用ロール(例: TRANSFORMER)を作るのが定石
      warehouse: COMPUTE_WH
      database: OLIST_DWH
      schema: DBT_DEV
      threads: 4
